# 2026-04-04 工作记录

## iPad端全屏预览优化（3项修改）

### 1. iPad全屏预览强制横屏
- **LEDFullScreenViewController**: `supportedInterfaceOrientations` / `preferredInterfaceOrientationForPresentation` 在iPad时返回landscape；`viewWillAppear`中调用 `UIDevice.current.setValue(.landscapeRight)` + `attemptRotationToDeviceOrientation()`
- **HeartGridViewController/ILoveUViewController/View520ViewController**: `viewWillAppear`中增加iPad强制横屏旋转代码
- **LoveRainViewController**: `supportedInterfaceOrientations` 增加iPad判断
- **FireworksBloomViewController/FireworksViewController**: 新增 `viewWillAppear` 中iPad横屏旋转 + `supportedInterfaceOrientations` iPad返回landscape
- **时钟类(Stopwatch/Countdown/FlipClock/DigitalClock)**: 已有完整的 `enforceLandscape()` 机制，无需额外修改

### 2. iPad全屏文字变大
- `LEDFullScreenViewController` 新增 `iPadFontScaleFactor` 属性（pad时=1.5，否则=1.0）
- 字体大小从 `ledItem.fontSize` 改为 `ledItem.fontSize * iPadFontScaleFactor`
- 即：iPad上72pt字体→108pt

### 3. 成为会员按钮 + 订阅解锁字号+4pt
- **成为会员按钮** (VIPPreviewOverlayView.becomeMemberButton): 16pt → 20pt (+4pt)
- **订阅解锁文字** (VIPPreviewOverlayView.subtitleLabel): 15pt → 19pt (+4pt)

## iPad端页面适配修改（3项）

### 4. iPad边框Tab内容挤压修复
- **LEDCreateViewController.swift `showTab`方法**: 
  - iPad端最小高度从800pt增大到1200pt
  - 使用动态 `minHeight`（iPad=1200, iPhone=800）
  - 确保边框Tab所有模块（LED边框图片、跑马灯、灯牌、线性）在iPad上不被挤压，可滚动展示完整

### 5. 封面文字大小调整
- **MyCreationsViewController.swift `CreationTableCell.configure`**: 
  - 封面字体基准宽度从852增大到1100
  - 缩放因子变小：`scaleFactor = containerWidth / 1100`（原为 `/852`）
  - 效果：封面文字更小，能完整显示不截断

### 6. 设置页面按钮区间距+10pt

## 编译错误修复（2项）

### 7. LEDFullScreenViewController UIInterfaceOrientation.allButUpsideDown 废弃API
- **文件**: `LEDFullScreenViewController.swift:110`
- **问题**: `UIInterfaceOrientation.allButUpsideDown` 在新版SDK中已不存在
- **修复**: 改为返回 `.landscapeRight`，与iPad端行为一致

### 8. ILoveUViewController isIGrid 未使用变量警告
- **文件**: `ILoveUViewController.swift:256`
- **问题**: `isIGrid` 变量被写入但从未读取
- **修复**: 移除该变量声明和赋值，保留 `isLetterGrid` 即可区分字母区域
- **SettingsViewController.swift stackView约束**:
  - 顶部间距: 60pt → 70pt (+10pt)
  - 底部间距: 80pt → 90pt (+10pt)

## iPad端页面优化（4项）

### 9. 封面文字再缩小
- **MyCreationsViewController.swift**（创作区卡片）: 基准宽度从1100→1400
- **TemplateSquareViewController.swift**（首页卡片，2处）: 基准宽度从852→1100
- 效果：封面文字进一步缩小约22%

### 10. 全屏预览强制横屏 + 文字大小调整
- **LEDFullScreenViewController.swift**:
  - 新增 `shouldAutorotate: true`
  - iPad和iPhone都强制 `.landscape`
  - iPad字体放大系数: 1.5 → 1.3

### 11. 首页卡片退出全屏预览后"编辑"文字残留bug修复
- **TemplateSquareViewController.swift `TemplateItemCell.configure`**:
  - configure开头重置：tryButton/titleLabel/buttonStack/previewButton全部隐藏

### 12. TabBar选中胶囊背景去除 + 间距放大
- **MainTabBarController.swift**: 彻底禁用iOS 17.4+ selectionIndicator胶囊背景
- **CustomSpacedTabBar.swift**: edgeInset 20→36, itemWidth 80→68

### 13. MainTabBarController selectionIndicator KVO崩溃修复
- **问题**: `tabBar.value(forKey: "selectionIndicator")` 在 `CustomSpacedTabBar` 上触发 `NSUnknownKeyException` 崩溃
- **原因**: 该KVO key在自定义TabBar类上不存在
- **修复**: 移除KVO访问，改为 `DispatchQueue.main.async` 中遍历 `tabBar.subviews`，通过类名匹配包含"Indicator"的子视图来安全隐藏

### 14. MainTabBarController 多余大括号修复
- **问题**: 第164行多余 `}` 导致 setupTabBar() 方法提前关闭，后续代码全部在类外部
- **原因**: 上次替换时引入的多余闭合括号
- **修复**: 删除第164行多余的 `}`

### 15. iPad设置页面按钮区间距再次+10pt（后又+15pt），最终改为isPad区分
- **SettingsViewController.swift** 全部改为 `isPad` 三元表达式：
  - **按钮区**: spacing (iPhone=12, iPad=22), top (iPhone=70, iPad=95), bottom (iPhone=-90, iPad=-115)
  - **VIP弹窗**: 宽度 (iPhone=320, iPad=360)
  - **评价弹窗**: 宽度 (iPhone=360, iPad=460)，间距全部isPad三元（closeButton/标题/消息/按钮/底部间距）
  - **语言弹窗**: 宽度 (iPhone=314, iPad=394)，最大高度(iPhone=620, iPad=680)，滚动区高度(iPhone=440, iPad=500)，内容边距和间距全部isPad三元
### 16. 首页卡片背景高度 + 文字位置优化
- **TemplateSquareViewController.swift TemplateItemCell**:
  - containerView 底部: 紧贴 → **-8pt**
  - titleLabel/tryButton 与图片间距: **8→6pt**
  - titleLabel/tryButton 底部边距: **-12→-10pt**

### 17. 版本号升级 1.2.6 → 1.2.7
- **project.pbxproj**: MARKETING_VERSION 全部改为 1.2.7
- **SettingsViewController.swift**: 默认版本号改为 1.2.7

### 18. 设置页面移除滚动 + 标题消失修复
- **问题**: scrollView 从 safeAreaLayoutGuide.topAnchor 开始，滚动时导航栏标题随内容滚出视野
- **修复**: 移除主设置页的 UIScrollView，直接将 stackView 放置在 view 上
  - stackView 从 safeAreaLayoutGuide.topAnchor 开始（iPad +25pt, iPhone +10pt）
  - 底部使用 lessThanOrEqualTo 约束，不超出安全区域
  - 不再可滚动，设置项一屏完整展示

### 19. Pad端线框图片加载 + Border Tab布局重构
- **LEDCreateViewController.swift**:
  - **Pad端线框**: `createLEDBorderImageStack` 增加 `isPad` 判断，iPad优先加载 `line_N_pad` 图片（4092×2048），回退到非pad版本
  - **Border Tab布局重构**: 从固定 `tabYOffset` 偏移方式改为 **垂直 UIStackView** 自动管理
    - 原问题：所有行都约束到 borderTabView.topAnchor，iPad上按钮宽度大→高度=width*9/16更大，但tabYOffset递增只有60pt，导致内容重叠堆叠
    - 新方案：使用 mainStack (vertical) 包含所有 sectionLabel + 边框行，StackView 自动计算每行的实际高度
  - 所有边框Stack间距改为 isPad 三元（iPhone=10pt, iPad=14pt）
  - 新增 `createSectionLabel()` 方法用于创建分区块标题
  - Border Tab 的 addSectionLabelToView 调用全部替换为 createSectionLabel + mainStack.addArrangedSubview

### 20. Pad端预览区比例优化 + 全屏预览拉伸修复 + 换行一致性修复
- **LEDCreateViewController.swift**:
  - **Pad端预览区宽高比**: 从固定 `9.0/19.5`（iPhone全面屏）改为 isPad 三元：iPad=`3.0/4.0`（横屏4:3比例），iPhone=`9.0/19.5`
  - **Pad端预览区左右边距**: 从60pt改为80pt
  - **Pad端字体缩放计算**: `fullScreenEffectiveHeight` iPad扣除60pt（vs iPhone的40pt），更准确反映iPad横屏实际可用高度
  - **创建页LED边框图片预览**: 增加 `_pad` 版本加载（isPad判断）+ contentMode 改为 `.scaleAspectFit`
- **LEDFullScreenViewController.swift**:
  - **LED边框图片全屏展示**: iPad优先加载 `line_N_pad` 图片，contentMode改为 `.scaleAspectFit`（不拉伸）
  - **背景图片**: iPad改用 `.scaleAspectFill`（等比填充不拉伸）
  - **文字换行逻辑统一**: 换行关闭时启用 `adjustsFontSizeToFitWidth=true` + `minimumScaleFactor=0.3`（与创建页预览行为一致），避免长文字被截断

### 21. Pad端全面比例优化（卡片封面+预览区+背景Tab+边框Tab按钮）
- **TemplateSquareViewController.swift**（首页卡片列表）:
  - **注意**: 首页卡片保持 **16:9** 比例不变，不需要改
  - Pad端封面圆角恢复为 **12pt**
- **LEDCreateViewController.swift**:
  - **Pad端预览区圆角**: 从15pt增大到24pt
  - **Pad端预览区宽高比**: 从 `3/4`(0.75) 改为 `0.7`，更准确匹配iPad横屏实际可用区域(约1194x834pt)
  - **Pad端预览区宽度**: 从左右各80pt边距（几乎全宽）改为 **屏幕宽度一半 + 居中显示**
    - 使用 centerXAnchor 约束居中 + widthAnchor = screenWidth / 2
  - **Pad端背景Tab素材按钮宽高比**: 从 `9/16` (16:9) 改为 `3/4` (3:4)
    - 涉及：`setupBackgroundTab` 的 buttonHeight计算、两个 `createTemplateStack` 方法中的 multiplier 约束
  - **Pad端边框Tab按钮宽高比**: 全部从 `9/16` 改为 `3/4`
    - 涉及：`createMarqueeBorderStack`、`createLightBoardBorderStack`、`createLinearBorderStack`、`createLEDBorderImageStack` 共4个方法

### 22. 首页卡片比例回退 + 创建页预览区宽度缩小
- **TemplateSquareViewController.swift**: 
  - 首页卡片封面比例**撤销**之前改为3:4的修改，恢复为 **16:9** (`9/16`)
  - 封面圆角恢复为 **12pt**（不再区分Pad/iPhone）
  - sizeForItemAt 和 heightForRowAt 的比例计算同步恢复为 **16:9**
- **LEDCreateViewController.swift**:
  - Pad端预览区布局从 leading/trailing ±80pt 改为 **centerXAnchor 居中 + width = screenWidth / 2**
  - 效果：预览区只占屏幕宽度的一半，下方有更多空间展示编辑选项
